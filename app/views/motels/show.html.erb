<div class="px-16 mt-12">
    <h4>Guest Detail: <%= @client.name %></h4>
    <section class="px-3 mt-16">
        <p>Additional Approved</p>
        <p>Guest Names</p>
        <ul class="mt-6">
            <li>Limp Bizkit</li>
            <li>Limp Bizkit</li>
            <li>Limp Bizkit</li>
            <li>Limp Bizkit</li>
        </ul>
    </section>
    <section class="mt-16">
        <h4>Past Incidents</h4>
        <div class="px-3 mt-8">
        <% if @client.incident_reports.any? %>
            <% @client.incident_reports.each do |incident| %>
                    <p><%= incident.description %></p>
            <% end %>
        <% else %>
            <p><strong>No incident reports to show</strong></p>
        <% end %>
        </div>
    </section>
</div>