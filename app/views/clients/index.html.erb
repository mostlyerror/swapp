<div class="px-4">
  <section class="mt-16">
    <p class="step-description">1. Search the red flag list</p>
    <h1 class="mt-24 text-9xl text-black font-bold">Start here. 🌱</h1>
    <p class="mt-12 help-text">
      Look up client to see if they have used SWAP before. Use broad search terms: "Ben" instead of "Benjamin," or "Jo" instead of "Joseph."
    </p>
  </section>

  <section class="mt-16 px-4">
    <%= render partial: 'search_form' %>
  </section>

  <main class="mt-16">
    <% if @searched %>

      <% if @clients.any? %>
        <section class="mt-16">
          <p class="help-text">
          <strong><%= @clients.size %> results found for "<%= @searched_term
            %>"</strong>. If you see your client in the table above,
        click on the row to check if they have been previously red flagged.
          </p>
        </section>

        <section class="mt-16">
          <%= render partial: 'table' %>
        </section>
      <% end %>

      <section class="py-16 px-4">

        <p class="help-text">
        <strong><%= @clients.size %> results found for "<%= @searched_term %>"</strong>.<br />
          If you think your client is already in Swapp, try searching for a different name.
        </p>
        <h2 class="mt-16 text-7xl font-bold">
          Still can't find your client?  Proceed to <%= link_to 'intake', new_intake_path, class: 'link' %>.
        </h2>
      </section>
    <% end %>
  </main>
</div>
