<% if @swap.present? %>
  <section id="notice--swap-inactive" class="notice bg-blue-300">
    <h3>SWAP is active.</h3>
    <p class="text-blue-900"><%= @swap.start_date.strftime("%a, %m/%d") %> through <%= @swap.end_date.strftime("%a, %m/%d") %></p>
  </section>
<% end %>
